{
  "name": "git-sith-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build:articles && npm run build:aoc",
    "build:articles": "ts-node scripts/build-articles.ts",
    "build:aoc": "ts-node scripts/build-aoc.ts",
    "screenshots": "node scripts/capture-screenshots.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "puppeteer": "24.33.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.0"
  }
}
