{
  "name": "git-sith-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build:articles && npm run build:aoc && npm run build:sitemap",
    "build:articles": "tsx scripts/build-articles.ts",
    "build:aoc": "tsx scripts/build-aoc.ts",
    "build:student-projects": "tsx scripts/build-student-projects.ts",
    "build:sitemap": "tsx scripts/build-sitemap.ts",
    "screenshots": "tsx scripts/capture-screenshots.ts",
    "lighthouse": "tsx scripts/run-lighthouse-local.ts",
    "lighthouse:prod": "tsx scripts/run-lighthouse-prod.ts"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "puppeteer": "24.33.1",
    "tsx": "^3.12.0",
    "typescript": "^5.4.0"
  }
}
